import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'dev.vabalas.gmoneymanager',
  appName: 'g-money-manager-app',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
